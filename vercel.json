{
  "version": 2,
  "framework": null,
  "installCommand": "npm install --include=dev",
  "buildCommand": "npm run build && echo '---DEBUG---' && pwd && ls -la dist/ 2>&1 || true && ls -la frontend/dist/ 2>&1 || true",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets/|css/|images/|favicon.ico|manifest.json|service-worker.js).*)",
      "destination": "/index.html"
    }
  ]
}
