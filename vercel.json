{
  "version": 2,
  "framework": null,
  "installCommand": "npm install --include=dev",
  "buildCommand": "npm run build && rm -rf dist && mv frontend/dist dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets/|css/|images/|favicon.ico|manifest.json|service-worker.js).*)",
      "destination": "/index.html"
    }
  ]
}
